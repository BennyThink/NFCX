# 19. GitHub Actions 运行时更新

## 实际完成内容

- 将所有工作流中的 `actions/checkout` 从 v4 更新至 v5。
- 将所有工作流中的 `actions/setup-go` 从 v5 更新至 v6。
- 将所有工作流中的 `actions/setup-node` 从 v4 更新至 v5。
- 保持 cache、artifact 和 MSYS2 actions 的现有主版本不变，因为它们已使用当前配置所需的版本。

## 实现过程中的问题

无。

## 阻塞或未完成事项

无。
