# 20 — Website FAQ

完成内容：在 `website/zh-CN/index.html` 新增中文 FAQ 段落，并使用原生 HTML `details`/`summary` 使答案默认折叠、点击展开。随后将同一组问题和答案翻译并加入英文首页 `website/index.html`；两页导航均新增 FAQ 锚点。共享样式在 `website/assets/css/site.css` 中提供 FAQ 的分隔、展开指示和移动端兼容展示。FAQ 的赞助入口指向 `https://github.com/sponsors/BennyThink`。同一组可折叠 FAQ 已同步至 `README.md` 与 `README.zh-CN.md`，并在两份 README 的项目链接中加入 GitHub Sponsors。中英文网站导航也分别新增 “Sponsor” 和“赞助”链接。FAQ 答案移除了 `max-width`，以便在宽屏中使用全部可用内容宽度。FAQ 答案内的链接改为高对比度的品牌青色，并保留清晰下划线。移除了 FAQ 后紧邻隐私区块的重复顶部边线。

实现过程中遇到的问题：无。

仍有被阻塞或未完成的事项：无。
